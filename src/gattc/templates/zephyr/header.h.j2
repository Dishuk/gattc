/* Auto-generated by gattc - DO NOT EDIT */

#ifndef {{ service_upper }}_H
#define {{ service_upper }}_H

#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include <zephyr/version.h>
#include <zephyr/sys/byteorder.h>
#include <zephyr/bluetooth/gatt.h>
#include <zephyr/bluetooth/uuid.h>

/* Minimum supported Zephyr version: 3.5.0 */
#if !defined(KERNEL_VERSION_NUMBER) || KERNEL_VERSION_NUMBER < 0x30500
#warning "gattc: Generated code is tested with Zephyr 3.5.0+. Older versions may have compatibility issues."
#endif

/*============================ Constants ============================*/

/* ATT notification header: opcode (1 byte) + handle (2 bytes) */
#define GATTC_ATT_HEADER_SIZE 3

/*============================ UUIDs ============================*/

#define {{ service_upper }}_UUID_VAL \
    {{ service_uuid_encoded }}
#define {{ service_upper }}_UUID \
    BT_UUID_DECLARE_128({{ service_upper }}_UUID_VAL)

{% for char in characteristics -%}
#define {{ service_upper }}_{{ char.name_upper }}_UUID_VAL \
    {{ char.uuid_encoded }}
#define {{ service_upper }}_{{ char.name_upper }}_UUID \
    BT_UUID_DECLARE_128({{ service_upper }}_{{ char.name_upper }}_UUID_VAL)

{% endfor -%}
/*============================ Payloads ============================*/

{% for payload in payloads -%}
{{ payload.content }}
{% endfor -%}
/*============================ Callbacks ============================*/

{% if has_callbacks -%}
/* Implement these in your code */
{% for cb in callbacks -%}
{{ cb }}
{% endfor -%}
{% endif -%}
#endif /* {{ service_upper }}_H */
