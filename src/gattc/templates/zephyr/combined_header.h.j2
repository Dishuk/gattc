/* Auto-generated by gattc - DO NOT EDIT */

#ifndef {{ header_guard }}
#define {{ header_guard }}

#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include <zephyr/version.h>
#include <zephyr/sys/byteorder.h>
#include <zephyr/bluetooth/gatt.h>
#include <zephyr/bluetooth/uuid.h>

/* Minimum supported Zephyr version: 3.5.0 */
#if !defined(KERNEL_VERSION_NUMBER) || KERNEL_VERSION_NUMBER < 0x30500
#warning "gattc: Generated code is tested with Zephyr 3.5.0+. Older versions may have compatibility issues."
#endif

/*============================ Constants ============================*/

/* ATT notification header: opcode (1 byte) + handle (2 bytes) */
#define GATTC_ATT_HEADER_SIZE 3

{% for svc in services %}
/*============================================================================*/
/*                           {{ svc.service_name | upper }}                              */
/*============================================================================*/

/*------------------------------ UUIDs ------------------------------*/

#define {{ svc.service_upper }}_UUID_VAL \
    {{ svc.service_uuid_encoded }}
#define {{ svc.service_upper }}_UUID \
    BT_UUID_DECLARE_128({{ svc.service_upper }}_UUID_VAL)

{% for char in svc.characteristics -%}
#define {{ svc.service_upper }}_{{ char.name_upper }}_UUID_VAL \
    {{ char.uuid_encoded }}
#define {{ svc.service_upper }}_{{ char.name_upper }}_UUID \
    BT_UUID_DECLARE_128({{ svc.service_upper }}_{{ char.name_upper }}_UUID_VAL)

{% endfor -%}
/*------------------------------ Payloads ------------------------------*/

{% for payload in svc.payloads -%}
{{ payload.content }}
{% endfor -%}
/*------------------------------ Callbacks ------------------------------*/

{% if svc.has_callbacks -%}
/* Implement these in your code */
{% for cb in svc.callbacks -%}
{{ cb }}
{% endfor -%}
{% endif -%}
{% endfor %}
#endif /* {{ header_guard }} */
